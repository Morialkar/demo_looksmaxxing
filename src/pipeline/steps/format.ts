import type { PipelineState } from "../state.js";

// Stub for Task 3. Task 8 replaces this with the real DraftPlan/fallback ->
// FinalPlan markdown rendering (including per-section article references).
export async function format(state: PipelineState): Promise<PipelineState> {
  return state;
}
