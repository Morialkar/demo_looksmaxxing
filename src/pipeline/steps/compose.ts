import type { PipelineState } from "../state.js";

// Stub for Task 3. Task 6 replaces this with the real LLM call + DraftPlan
// parse/retry.
export async function compose(state: PipelineState): Promise<PipelineState> {
  return state;
}
