import type { PipelineState } from "../state.js";

// Stub for Task 3. Task 5 replaces this with the curated in-memory corpus
// lookup.
export async function retrieve(state: PipelineState): Promise<PipelineState> {
  return state;
}
