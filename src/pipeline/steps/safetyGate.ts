import type { PipelineState } from "../state.js";

// Stub for Task 3. Task 4 replaces this with the real age/extreme-language
// routing logic.
export async function safetyGate(state: PipelineState): Promise<PipelineState> {
  return state;
}
